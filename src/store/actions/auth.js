import * as actionTypes from './actionsTypes';

export const auth = (email, password, isSignUp) => {
	return (dispatch, getState) => {
		// make async call (wait for respond) and then...
		dipspatch()
	}
}